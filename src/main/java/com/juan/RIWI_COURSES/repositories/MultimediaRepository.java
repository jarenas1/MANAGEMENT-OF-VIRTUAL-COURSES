package com.juan.RIWI_COURSES.repositories;

import com.juan.RIWI_COURSES.entities.MultimediaEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MultimediaRepository extends JpaRepository<MultimediaEntity, String> {
    //Atributos de MultimediaRepository
    //Constructores de MultimediaRepository
    //Asignadores de atributos de MultimediaRepository (setters)
    //Lectores de atributos de MultimediaRepository (getters)
    //Métodos de MultimediaRepository
}
