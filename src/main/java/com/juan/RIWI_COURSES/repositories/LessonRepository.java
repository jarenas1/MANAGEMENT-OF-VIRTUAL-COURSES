package com.juan.RIWI_COURSES.repositories;

import com.juan.RIWI_COURSES.entities.LessonEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LessonRepository extends JpaRepository<LessonEntity, String> {
    //Atributos de LessonRepository
    //Constructores de LessonRepository
    //Asignadores de atributos de LessonRepository (setters)
    //Lectores de atributos de LessonRepository (getters)
    //Métodos de LessonRepository
}
