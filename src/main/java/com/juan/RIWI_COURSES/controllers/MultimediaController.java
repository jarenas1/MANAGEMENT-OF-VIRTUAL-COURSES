package com.juan.RIWI_COURSES.controllers;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("")
public class MultimediaController {
    //Atributos de MultimediaController
    //Constructores de MultimediaController
    //Asignadores de atributos de MultimediaController (setters)
    //Lectores de atributos de MultimediaController (getters)
    //Métodos de MultimediaController
}
