package com.juan.RIWI_COURSES.services.CRUD;

public interface Save<Entity> {
    //Atributos de Save
    //Constructores de Save
    //Asignadores de atributos de Save (setters)
    //Lectores de atributos de Save (getters)
        //Métodos de Save
    public Entity save(Entity entity);  //Imagino que no es vacía, para devolver el id de algún registro
}
