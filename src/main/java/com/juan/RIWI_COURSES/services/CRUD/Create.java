package com.juan.RIWI_COURSES.services.CRUD;

public interface Create<EntityRequest, Entity> {
    //Atributos de Create
    //Constructores de Create
    //Asignadores de atributos de Create (setters)
    //Lectores de atributos de Create (getters)
        //Métodos de Create
    public Entity create(EntityRequest entityRequest);
}
