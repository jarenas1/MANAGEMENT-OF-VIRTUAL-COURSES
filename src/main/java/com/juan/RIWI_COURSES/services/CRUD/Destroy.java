package com.juan.RIWI_COURSES.services.CRUD;

public interface Destroy<ID> {
    //Atributos de Destroy
    //Constructores de Destroy
    //Asignadores de atributos de Destroy (setters)
    //Lectores de atributos de Destroy (getters)
        //Métodos de Destroy
    public void destroy(ID id);
}
